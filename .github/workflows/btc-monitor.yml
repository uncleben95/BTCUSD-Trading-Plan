name: BTC Monitor

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Ping BTC API
        run: |
          curl -fsS --retry 2 --max-time 30 \
            https://YOUR-VERCEL-DOMAIN.vercel.app/api/btc
